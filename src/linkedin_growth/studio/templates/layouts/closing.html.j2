<h2 class="h-closing" data-fit data-min="60">{{ s.slide.headline | inline }}</h2>
{% if s.slide.question %}<p class="closing-question" data-fit data-min="34">{{ s.slide.question | inline }}</p>{% endif %}
{% if s.slide.cta %}<p class="closing-cta">{{ s.slide.cta | inline }}</p>{% endif %}
